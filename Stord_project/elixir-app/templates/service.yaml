apiVersion: v1
kind: Service
metadata:
  name: {{ include "Elixir_app.fullname" . }}
  labels:
    {{- include "Elixir_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Elixir_app.selectorLabels" . | nindent 4 }}
